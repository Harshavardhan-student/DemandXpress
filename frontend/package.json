{
  "name": "contact-book-frontend",
  "version": "1.0.0",
  "description": "Frontend for Contact Book application",
  "main": "index.html",
  "scripts": {
    "start": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "lite-server": "^2.6.1"
  }
}